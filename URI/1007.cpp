#include <cstdlib>
#include <iomanip>
#include <iostream>
using namespace std;

int main(int argc, char *argv[]) {

    int a, b, c, d;

    cin >> a;
    cin >> b;
    cin >> c;
    cin >> d;

    cout << "DIFERENCA = " << (a * b - c * d) << endl;

    system("PAUSE");
    return EXIT_SUCCESS;
}
