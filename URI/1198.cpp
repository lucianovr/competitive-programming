#include <cmath>
#include <cstdlib>
#include <iostream>
using namespace std;

int main() {
    long long int a, b;

    while (cin >> a >> b)
        cout << abs(a - b) << endl;

    // system("PAUSE");
    return 0;
}
