#include <bits/stdc++.h>

using namespace std;

int main() {
    int n;
    while (cin >> n) {
        while (n--) {
            string s;
            int f;
            cin >> s >> f;
            if (s == "Thor")
                cout << "Y\n";
            else
                cout << "N\n";
        }
    }
    return 0;
}